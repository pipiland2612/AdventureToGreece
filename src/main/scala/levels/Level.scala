package levels

import Enemy.Enemy
import items.Item

class Level:
  var enemies: List[Enemy] = List()
  var items: List[Item] = List()
  var obstacles: List[Obstacle] = List()
  
  def spawnEnemy(enemy: Enemy): Unit = {
    
  }
  
  def generateObstacle(obstacle: Obstacle): Unit = {
    
  } 
  
//  def checkCollision()
  def updateLevel(): Unit = {
    
  }
  
  